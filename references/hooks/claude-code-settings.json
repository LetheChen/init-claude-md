{
  "_comment": "把以下 hooks 段合并到 ~/.claude/settings.json（合并而非覆盖整个文件）",
  "_verify": "python -m json.tool ~/.claude/settings.json  # 验证 JSON 合法",
  "hooks": {
    "SessionEnd": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"\$(git rev-parse --show-toplevel)/scripts/check-session-end.sh\""
          }
        ]
      }
    ]
  }
}